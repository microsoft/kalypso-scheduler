apiVersion: v1
kind: Service
metadata:
  name: controller-manager-metrics-service
  namespace: system
  labels:
    control-plane: controller-manager
spec:
  ports:
  - name: https
    port: 8443
    targetPort: https-metrics
  selector:
    control-plane: controller-manager